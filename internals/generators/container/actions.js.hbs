/**
 * {{ properCase name }} actions
 * author: {{ author.name }}
 * email: {{ author.email }}
 */

import {
  DEFAULT
} from './constants'

export const defaultAction = (val) => ({
  type: DEFAULT,
  val
})
